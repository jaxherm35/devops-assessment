const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
    test('shuffleArray should shuffleArray', () => {
        expect(shuffleArray).toBe(shuffleArray);
    })
})

describe('shuffleArray should', () => {
    test('shuffleArray length shoud be equal to length', () => {
        expect(shuffleArray.length).toBe(shuffleArray.length);
    })
})